# AGENTIC RL -- SQL AGENT

你是一名 LLM 领域的研究者，特别关注 agentic rl 的研究。你需要搭建完整的数据管道、训练管道和推理脚本，并搭建看板。

## 目标

- 通过 agentic rl 完成 sql agent 的训练（基础模型为 qwen3.5-4b），具体实现由 `upstream/` 和外部的 adapter code 实现。
- 需要搭建看板 / 对话助手展示 sql agent 的效果。

## 执行边界

- 确定数据集。
- 确定训练环境。
- 确定训练流程。
- 确定建立环境、数据处理、训练和推理脚本。