# Qwen3.5-4B SQL Agent 训练与展示方案

项目面向大模型 Agent 算法岗位，以 BIRD-RL 为代码基础、Qwen3.5-4B 为模型底座、verl 为训练框架，完成 SFT、多轮 GRPO 和可交互的效果展示。核心实验问题是：在相同数据、工具和推理预算下，多轮 RL 能否提高 SQL 任务成功率，并改善执行反馈纠错行为。

交付物是一套可复现训练配置、Base/SFT/SFT+RL 模型产物、独立评测报告，以及一个同时支持实时对话和实验对比的应用。数据、模型 revision 和训练环境已经固定，实施真值见 [DATA_ENVIRONMENT.md](DATA_ENVIRONMENT.md)。超参数、资源占用和工期仍是拟定起点，尚无真实 GPU 训练结果。

用户已有 AIGC、多模态微调和腾讯 QQ 团队实习经历。这个项目补充在线 RL、训练环境设计和实验分析经验；简历中应将其列为独立项目，使用实际测得的结果。

1. **技术选型与复用范围**

| 层次 | 选择 | 项目中的工作 |
|---|---|---|
| 上游代码 | BIRD-RL | 复用数据处理、SQL 工具、奖励、轨迹和推理流程 |
| 底座 | Qwen/Qwen3.5-4B 官方后训练权重 | 适配模板、工具解析与 checkpoint |
| 训练 | verl；LoRA SFT + 多轮 GRPO | 配置单卡训练，验证模型及适配器同步 |
| 模型服务 | vLLM | 提供统一模型推理接口 |
| 应用接口 | OpenAI compatible API + 本地 runner | 管理工具循环、任务执行、轨迹与评测记录 |
| 界面 | Streamlit | 对话、模型对比、指标图表和轨迹回放 |
| 存储 | JSONL/Parquet + SQLite 元数据 | 保存实验结果和可追溯的运行标识 |
| 计算 | AutoDL 单张 A100 80GB | 顺序执行训练、批量评测和在线演示 |

[BIRD-RL 官方仓库](https://github.com/bird-bench/BIRD-RL)已提供分阶段 SQL 训练流程。[verl 发布记录](https://github.com/verl-project/verl/releases)包含 Qwen3.5 训练支持，但两者的具体版本组合、单卡多轮 LoRA 仍需联调。[Qwen3.5-4B 模型卡](https://huggingface.co/Qwen/Qwen3.5-4B)作为模型格式和推理配置的依据。

固定上游 commit，将适配配置与必要补丁放在独立目录，并记录补丁原因。当前目录的自建 sql_agent 只用作协议和本地工具测试参考。正式训练、评测和在线推理统一复用经过适配的 BIRD-RL 工具执行层，避免产生两套行为不同的 SQL 环境。

2. **任务范围与数据**

首版主任务是只读 SQLite Text-to-SQL：输入自然语言问题、数据库 schema 及数据集提供的辅助说明，模型执行查询、观察结果、修改 SQL 并提交。辅助任务是对只读错误 SQL 的修复。SQL 调试数据中涉及数据库写入或模式修改的任务不混入首版只读评测，筛选规则与样本数必须公开记录。

主任务使用 BIRD 公开训练集；调试扩展参考 BIRD-RL 指向的 SIX-GYM-SQLite 训练数据与 BIRD-CRITIC-SQLite。两类任务各自使用匹配的数据格式、工具和评分器，分别报告指标。来源见 [BIRD 数据入口](https://bird-bench.github.io/)与 [BIRD-RL 数据说明](https://github.com/bird-bench/BIRD-RL)。

训练记录固定为 `birdsql/bird23-train-filtered@40684698` 的 6,601 条数据。内部划分使用 seed `20260911`，按数据库隔离训练池和验证池。SFT 来源池为 1,500 条训练记录和 150 条验证记录，实际 SFT 数量由正确教师轨迹数决定。GRPO 使用 2,000 条训练记录和 200 条验证记录。划分算法、清单和数据下载方式见 [DATA_ENVIRONMENT.md](DATA_ENVIRONMENT.md)。

最终评测使用冻结的 BIRD mini-dev，开发时不根据其中的个案调整提示词、奖励或训练数据。界面内置演示集从单独的展示数据中选择；若展示最终评测样本，必须在冻结实验后进行。自由输入的用户问题没有标准答案，界面只报告执行状态和结果，不显示“回答正确”。

SFT 优先复用上游可用且格式匹配的轨迹。若缺少轨迹，复用数据生成脚本生成并执行验证，单独统计教师模型调用成本。教师可读取训练标签用于构造示范，但参考答案字段与评测标签不得进入学生的运行时输入或工具返回。

3. **训练路线与单卡验证**

模型路线为：官方 Qwen3.5-4B → 多轮工具使用 SFT → 多轮 GRPO → 验证集选择 checkpoint → 冻结评测。BIRD-RL 的原始路线包含四个阶段；本项目先采用上述简化路线，因此不能将结果称为原论文完整复现。若基线暴露 SQL 能力不足，再讨论是否增加单轮训练阶段。

| 环节 | 拟定起点 | 继续条件 |
|---|---|---|
| 兼容性验证 | 小批量推理、一次 SFT 更新、一次 GRPO 更新 | 工具返回正常，更新后的权重能进入 rollout |
| 未训练基线 | 固定小验证集，自主工具调用 | 确认真实失败类型与成功/失败轨迹分布 |
| 多轮 SFT | LoRA rank 32；保留视觉模块但冻结；仅训练语言部分适配器 | 工具格式与验证集任务表现可用 |
| GRPO 小试 | 每题 4 条轨迹，micro batch 1，有效问题 batch 4-8 | 完成 10-20 个训练 step，记录显存与吞吐 |
| 正式 GRPO | 依据小试扩大样本与训练步数 | 验证集表现改善，训练无持续异常 |
| 扩展实验 | 正确性奖励上增加小幅调用成本项 | 检查效率收益和准确率损失 |

有效问题 batch 与每题轨迹数分开记录。起步上下文预算约 8K，最长六次探索调用，最终提交单独处理；实际 token 配置按选定 verl 版本的定义校准。若截断比例高，先分析 schema 和观测长度，再决定压缩观测或增加上下文，不能静默剔除困难样本。

首轮尝试关闭思考模式，固定训练和推理模板；如果上游轨迹格式不兼容，先完成转换与小样本验证。训练只计算 assistant 生成 token 的损失，工具和用户 token 作为上下文。训练温度与评测温度分别记录，三组模型的评测配置相同。

SFT checkpoint 作为 RL 初始策略及 KL 参考。如果采用 LoRA，必须冻结正确的参考适配器，不能误将无适配器的原始模型当作 SFT 参考。验证训练后 adapter 确实加载到采样端，并检查保存、重载后的输出一致性。

上游现有脚本需要明确适配：[多轮 SFT 脚本](https://github.com/bird-bench/BIRD-RL/blob/main/scripts/train/stage2_multiturn_sft.sh)默认四卡，且将 TRAIN_DATA 同时设置为 val_files；应改用独立验证集。[Agentic RL 脚本](https://github.com/bird-bench/BIRD-RL/blob/main/scripts/train/stage4_agentic_rl.sh)默认调试奖励和较大的数据库副本池；Text-to-SQL 应选择匹配的生成任务奖励，并按单卡并发调整资源。BIRD-RL 的 agent-loop 补丁也需核对所选 verl API。

先实测一个完整小试的耗时、峰值显存、轨迹数和 token 数，再估算正式训练费用。A100 80GB 是资源规划，尚未验证该组合能容纳目标上下文与并发。首版顺序运行 SFT、RL 和推理，不常驻三份模型，也不同时运行训练与在线展示。

4. **奖励与效果验证**

首先复现所选 BIRD-RL 任务的奖励与评分流程，再增加独立消融。生成任务依据官方执行评测；调试任务依据匹配的测试用例。SQL 执行成功只是中间状态，不能直接等同于语义正确。参考 SQL、参考结果和测试用例仅由隔离的评测器访问。

扩展奖励可以采用 R = S × (1 − λ × c)：S 是任务是否成功，c 是归一化工具成本，0 ≤ c ≤ 1，λ 为小于 1 的候选权重。先使用 λ = 0 建立基线，再只在验证集选择成本权重。它是实验设计，不宣称新的强化学习算法。

若同题轨迹全失败，GRPO 的组内优势可能缺少有效信号。记录全失败组比例、奖励方差、截断率、无效工具率、KL、生成长度和调用次数。通过冷启动、难度调整或采样策略改善信号时，要同步保留对应对照。

| 对照 | 作用 | 优先级 |
|---|---|---|
| Base + 固定生成/执行/纠错流程 | 判断手工流程的效果 | 扩展 |
| Base + 自主工具 Agent | 测原始模型的多轮能力 | 首版 |
| SFT + 相同 Agent | 测示范轨迹带来的变化 | 首版 |
| SFT + 多轮 GRPO + 相同 Agent | 测在线 RL 的增量 | 首版 |
| SFT + 单轮 RL | 区分 SQL 能力提升与交互训练收益 | 扩展 |
| 多轮 GRPO + 成本奖励 | 测准确率与调用成本的关系 | 扩展 |

三组首版对照共享任务集、数据说明、工具实现、上下文和 token 预算，固定评测思考模式。主要指标为执行准确率和调试成功率，另外统计每任务调用次数、总生成 token、失败类型、推理耗时。延迟对比排除模型加载与队列等待，并分别记录这些额外耗时；缓存和预热条件相同。

纠错对比使用相同的错误初始 SQL 与任务集合。预算实验分别设置 2、4、6 次探索调用。对关键结果做三次独立训练种子复验，并报告任务级配对 bootstrap 置信区间；两者分别反映训练波动与任务采样不确定性。如果算力只允许一个种子，报告必须明确标注。

5. **展示应用：对话、对比与分析**

首版使用 Streamlit 构建单用户研究演示应用，复用其 [聊天组件](https://docs.streamlit.io/develop/api-reference/chat)。界面包含三个视图，通过同一份 run/episode 记录串联。训练曲线作为实验分析的次级视图，主界面优先展示用户问题、SQL 和查询结果。

| 视图 | 用户操作 | 实际展示内容 |
|---|---|---|
| SQL 对话 | 选择数据库和模型，输入问题，继续追问 | 流式状态、最终 SQL、结果表格、工具轨迹、耗时与 token |
| 模型对比 | 选择固定题目，一次提交三组比较 | Base/SFT/SFT+RL 的独立轨迹、最终 SQL、已评测正确性、成本 |
| 实验看板 | 选择评测批次，按难度/数据库/错误类型筛选 | 准确率、纠错成功率、成本曲线、失败样例与 checkpoint 信息 |

对话视图左侧放数据库选择、表结构和模型选择；中间显示会话与结果；右侧展开工具调用。查询结果支持表格展示与导出。数值/分类列可用确定性规则生成柱状图或折线图，图表数据必须来自实际查询结果；首版无需额外模型生成图表代码。

多轮追问可复用当前会话，例如“只看北京”和“改成按月统计”。用户连续对话与模型内部多次工具交互是两种不同能力：BIRD 单问题训练不能证明前者已经改善。首版保留对话功能，另建独立多轮用户测试集再报告追问能力。

工具轨迹显示工具名称、SQL 参数、数据库返回摘要、错误信息及后续修正。每个步骤来自真实执行事件，不用生成动画补造步骤。可以用简短状态说明表达“查询中”“执行报错”“正在重试”，避免把事后解释当作模型原始推理。

对比视图可选任意两组或三组模型。相同用户输入和先前对话快照分别传入各组，各自继续执行，彼此不可读取另一组的 SQL 或工具结果。单卡首版排队执行，界面明确显示排队、运行和完成状态。已完成结果可并排查看，即使底层是顺序推理。

优先验证相同底座上的多 LoRA 服务，参考 [vLLM LoRA 文档](https://docs.vllm.ai/en/latest/features/lora/)。只有确认 Qwen3.5 和目标适配器可正确加载后才启用；否则分别导出并顺序加载 checkpoint。SFT+RL 的导出必须包含完整适配器状态，不能丢失 SFT 权重或把两个不兼容 adapter 简单叠加。

看板同时支持实时运行与历史回放。历史结果清楚标注“历史回放”，保留运行时间与 checkpoint ID。实时运行失败时显示失败原因，可由用户切换到回放；系统不自动伪装成成功的实时结果。没有评测记录时显示空状态，零分与未运行严格区分。

6. **训练与展示共用的数据接口**

| 记录 | 必需字段 | 用途 |
|---|---|---|
| Model | model_id、base_revision、adapter/checkpoint_path、训练配置摘要 | 防止 Base、SFT、RL 标签与实际权重不符 |
| Run | run_id、模型标识、数据划分哈希、数据库快照哈希、工具版本、采样参数 | 重建评测条件 |
| Episode | episode_id、task_id、输入、最终 SQL、状态、耗时、用量 | 单任务对比与筛选 |
| Event | 序号、类型、时间、工具名、参数、观测摘要、错误 | 流式显示与轨迹回放 |
| Evaluation | 评分器版本、是否有标签、正确性、失败原因 | 区分执行成功和答案正确 |

建议事件类型为 queued、model_started、tool_called、tool_result、sql_submitted、evaluation_finished、failed。模型接口未提供 token 用量时记录 null，不能填 0。敏感认证信息不进入事件或模型响应存档。

vLLM 提供 OpenAI compatible API，Agent runner 负责工具调度和记录，评分器在 rollout 结束后独立运行。Streamlit 调用 runner 并读取 JSONL 历史结果；UI 无权向 Agent 注入 gold。

7. **演示故事与验收标准**

准备少量经过复核的样例，覆盖简单查询、多表关联、聚合、执行失败后修复，以及训练后仍然失败的情况。重点展示至少一个 Base/SFT 失败而 RL 成功的真实样例；如果没有找到或全量指标不支持提升，照实呈现，不能编造预设的成功故事。

一段三分钟演示按顺序完成：选择公开示例数据库并提问；查看 SQL 与结果；打开同题对比；定位一次真实错误和修复；进入看板查看全量结果和局限。精选案例仅用于解释行为，全量冻结评测集承担效果结论。

| 验收项 | 完成条件 |
|---|---|
| 上游复用 | 固定 BIRD-RL/verl revision，补丁和配置可以重建 |
| 训练 | SFT 与 GRPO 实际运行，日志可追溯，checkpoint 可重载 |
| 对照 | Base、SFT、SFT+RL 在同一冻结数据上完成评测 |
| 对话 | 输入问题后确实调用选定模型与数据库，返回 SQL 和查询结果 |
| 对比 | 独立运行且无相互信息泄露，每组绑定实际 checkpoint |
| 看板 | 所有指标由实验记录计算，支持定位到具体任务轨迹 |
| 评分 | 自由问题无伪造正确性标签，工具返回不泄露 gold |
| 复现 | 保存环境版本、种子、数据清单、模型标识和评测入口 |

首版使用固定公开样例数据库，只读查询有超时和返回大小限制。会话独立，断开或取消后释放数据库资源。界面与模型通过 SSH 转发访问；多租户、任意数据库上传和公网发布不属于这次单人演示的必要工作。

8. **进度与成本控制**

| 时间估计 | 工作 | 可检查产物 |
|---|---|---|
| 第 1 周 | 克隆并固定上游、适配 4B、单卡兼容性验证、冻结数据划分 | 可运行小试、资源测量、依赖清单 |
| 第 2 周 | Base 评测、SFT 数据与训练、对话界面接入 Base | Base 结果、SFT checkpoint、真实对话 |
| 第 3 周 | 多轮 GRPO 小试及首轮训练、打通统一轨迹记录 | RL checkpoint、训练诊断、可回放记录 |
| 第 4 周 | 三组冻结评测、模型对比界面、错误分析 | 对比表、真实案例、独立模型运行 |
| 第 5 周 | 预算消融、关键实验复验、实验看板 | 成本曲线、波动分析、可筛选看板 |
| 第 6 周 | 重载复现、界面检查、整理报告与演示 | 完整仓库、模型产物、演示视频 |

工期按单人持续投入估计。若依赖兼容性或数据获取超出预期，优先保留三组主对照、实时对话与历史回放；扩展消融后置。AutoDL 的实际租价、磁盘费用和教师模型费用在资源确定后单独核算，不预先承诺总价。

申请 GPU 后先记录试跑每步墙钟时间、峰值显存、有效轨迹数与失败比例。正式预算按计划训练步数、复验次数、评测运行和环境调试开销计算。历史回放与图表可在 Mac 上运行，只有实时推理或训练时需要 GPU。

最终求职材料应包含代码贡献边界、模型训练记录、全量对照结果及可操作应用。简历中的准确率提升、调用成本下降和训练开销均从冻结实验记录填写。
