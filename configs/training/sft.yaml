method: lora_sft
base: models/Qwen3.5-4B
jsonl: outputs/sft/messages.jsonl
output_dir: outputs/sft
lora_rank: 16
lora_alpha: 32
learning_rate: 2.0e-4
epochs: 1
max_seq_len: 8192
micro_batch_size: 1
max_steps: 20
min_rows: 16
loss: assistant_tokens_only
status: not_run
note: Optional LoRA on a messages jsonl. Default training is GRPO from the base model, matching PaperSearchQA / Search-R1. Do not treat this file as a required cold start.
