# CPU runtime. Versions are lower bounds, not a verified lockfile.
# Training stack (torch / vLLM / verl) lives in requirements-train.txt and
# pyproject.toml [project.optional-dependencies] train. Do not put CUDA wheels here.
fastapi>=0.115
httpx>=0.27
pydantic>=2.7
pyyaml>=6.0
uvicorn>=0.32
